@model IEnumerable<LibraryWebApp.Models.equipment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equip_type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equip_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equip_director)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equip_genre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equip_length)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.last_checkout)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.last_return)
        </th>
        <th>
            @if (User.IsInRole("Library Admin") || User.IsInRole("Library Staff"))
            {
                @Html.DisplayNameFor(model => model.checkout_records1.transaction_id)
            }
        </th>
        <th>
            @if (User.IsInRole("Library Admin") || User.IsInRole("Library Staff"))
            {
                @Html.DisplayNameFor(model => model.checkout_records2.transaction_id)
            }
            </th>
        <th>
            @if (User.IsInRole("Library Admin") || User.IsInRole("Library Staff"))
            {
                @Html.DisplayNameFor(model => model.checkout_records3.transaction_id)
            }
            </th>
        <th>
            @if (User.IsInRole("Library Admin") || User.IsInRole("Library Staff"))
            {
                @Html.DisplayNameFor(model => model.checkout_records4.transaction_id)
            }
            </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equip_type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equip_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equip_director)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equip_genre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equip_length)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.last_checkout)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.last_return)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.checkout_records1.transaction_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.checkout_records2.transaction_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.checkout_records3.transaction_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.checkout_records4.transaction_id)
        </td>
        <td>
            @if (User.IsInRole("Library Admin") || User.IsInRole("Library Staff"))
            {
                @Html.ActionLink("Edit", "Edit", new { id = item.equip_id })
                @Html.ActionLink("Delete", "Delete", new { id = item.equip_id })
            }
            @Html.ActionLink("Details", "Details", new {id = item.equip_id})
        </td>
    </tr>
}

</table>
